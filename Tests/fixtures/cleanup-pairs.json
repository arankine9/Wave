{
  "version": 1,
  "description": "Raw spoken transcript paired with the cleaned-up code/text the model should produce. Used by the gate-decision test today and by the LLM judge once a backend is wired in.",
  "entries": [
    { "raw": "open paren self dot user underscore id close paren", "cleaned": "(self.user_id)", "expectGateSkip": false },
    { "raw": "function add a b return a plus b", "cleaned": "function add(a, b) { return a + b }", "expectGateSkip": false },
    { "raw": "if user underscore id equals null", "cleaned": "if (user_id == null)", "expectGateSkip": false },
    { "raw": "self dot configure dot api underscore key", "cleaned": "self.configure.api_key", "expectGateSkip": false },
    { "raw": "import numpy as np", "cleaned": "import numpy as np", "expectGateSkip": false },
    { "raw": "let x equals five", "cleaned": "let x = 5", "expectGateSkip": false },
    { "raw": "const handler equals async function event", "cleaned": "const handler = async (event) => {}", "expectGateSkip": false },
    { "raw": "for i in range ten", "cleaned": "for i in range(10):", "expectGateSkip": false },
    { "raw": "no wait make it a function", "cleaned": "// rephrase: turn it into a function", "expectGateSkip": false },
    { "raw": "return null", "cleaned": "return null", "expectGateSkip": false },
    { "raw": "console dot log result", "cleaned": "console.log(result)", "expectGateSkip": false },
    { "raw": "struct user has name and age", "cleaned": "struct User { var name: String; var age: Int }", "expectGateSkip": false },
    { "raw": "enum status idle running done", "cleaned": "enum Status { case idle, running, done }", "expectGateSkip": false },
    { "raw": "throw new error invalid input", "cleaned": "throw new Error('invalid input')", "expectGateSkip": false },
    { "raw": "await fetch slash users", "cleaned": "await fetch('/users')", "expectGateSkip": false },
    { "raw": "if x equals one print yes else print no", "cleaned": "if x == 1: print('yes') else: print('no')", "expectGateSkip": false },
    { "raw": "lambda x colon x times two", "cleaned": "lambda x: x * 2", "expectGateSkip": false },
    { "raw": "open bracket one comma two comma three close bracket", "cleaned": "[1, 2, 3]", "expectGateSkip": false },
    { "raw": "open brace name colon alex close brace", "cleaned": "{ name: 'alex' }", "expectGateSkip": false },
    { "raw": "from datetime import datetime", "cleaned": "from datetime import datetime", "expectGateSkip": false },
    { "raw": "def fibonacci of n", "cleaned": "def fibonacci(n):", "expectGateSkip": false },
    { "raw": "var counter equals zero", "cleaned": "var counter = 0", "expectGateSkip": false },
    { "raw": "this dot state dot count", "cleaned": "this.state.count", "expectGateSkip": false },
    { "raw": "pipe pipe default value", "cleaned": "|| default_value", "expectGateSkip": false },
    { "raw": "try catch error log error", "cleaned": "try { ... } catch (error) { log(error) }", "expectGateSkip": false },
    { "raw": "alright on it", "cleaned": "alright on it", "expectGateSkip": true },
    { "raw": "send the file", "cleaned": "send the file", "expectGateSkip": true },
    { "raw": "yes that works", "cleaned": "yes that works", "expectGateSkip": true },
    { "raw": "tomorrow morning", "cleaned": "tomorrow morning", "expectGateSkip": true },
    { "raw": "thanks for the help", "cleaned": "thanks for the help", "expectGateSkip": true },
    { "raw": "doting parents are wonderful", "cleaned": "doting parents are wonderful", "expectGateSkip": true }
  ]
}
